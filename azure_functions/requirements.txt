# Azure Functions Core (OBRIGATÓRIO) - versão específica para v2 programming model
azure-functions>=1.23.0

# Dependências principais do projeto G-Click
requests>=2.31.0
PyYAML>=6.0
python-dotenv>=0.19.0
backoff>=1.11.1
python-dateutil>=2.8.2

# Bot Framework
botbuilder-core>=4.14.0
botbuilder-schema>=4.14.0
botframework-connector>=4.14.0

# HTTP Client assíncrono
aiohttp>=3.8.1

# Logging e utilitários
python-json-logger>=2.0.0

# Dependências opcionais para relatórios - não são necessárias em runtime se os
# módulos de relatório não forem utilizados. Adicione em requirements-dev.txt se quiser
# ter disponível para desenvolvimento.
# pandas>=2.0.0  # Necessário para gerar relatórios em excel
# openpyxl>=3.0.0  # Necessário para pandas exportar em xlsx

# Werkzeug para compatibilidade (removido para reduzir memory footprint)
# werkzeug~=3.1.3

# NOVO: Para timezone (OBRIGATÓRIO para zoneinfo no Linux)
tzdata>=2023.3
# Relatórios Excel (comentado para reduzir memory footprint)
# pandas>=2.2
# openpyxl>=3.1
backports.zoneinfo>=0.2.1; python_version<"3.9"
